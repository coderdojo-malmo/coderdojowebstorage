<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
    <title><%= h @title || "CoderDojo Malmö - Web storage" %></title>
    <link rel="stylesheet" type="text/css" href="/css/style.css"/>
    <script type="text/javascript" src="/js/jquery.js"></script>
  </head>
  <body>
    <header class="clearfix">
    <div class="container">
      <div class="navbar">
        <div class="navbar-inner">
          <a class="brand" href="/">CoderDojo Hemsideutrymme</a>
          <ul class="nav pull-right">
          <% if is_authenticated? %>
              <% if is_admin? %>
                <li><a href="/handleusers">Hantera användare</a></li>
              <% end %>
              <li>
                <form method="post" action="/signout" class="navbar-form">
                  <input type="hidden" name="_method" value="delete"/>
                  <input type="submit" class="btn btn-primary" value="Logga ut"/>
                </form>
              </li>
          <% else %>
              <li><a href="/signin" >Logga in</a> </li>
              <li><a href="/signup" >Registrera dig</a></li>
          <% end %>
          </ul>
        </div>
      </div>
    </div>
    </header>
    <div class="container">
      <% [:notice, :error].each do |flash_type|
        if flash.has? flash_type  %>
          <div class="alert alert-<%=h flash_type%>">
            <%= h flash[flash_type]%>
          </div>
        <% end
      end %>
      <%= yield %>
    </div>
    <script type="text/javascript" src="/js/bootstrap.min.js"></script>
    <script type="text/javascript" src="/js/application.js"></script>
  </body>
</html>
